
get_filename_component(mpark_variant_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${mpark_variant_CMAKE_DIR})

list(REMOVE_AT CMAKE_MODULE_PATH -1)

if(NOT TARGET mpark_variant)
    include("${mpark_variant_CMAKE_DIR}/MparkVariantTargets.cmake")
endif()

set(mpark_LIBRARIES mpark_variant)
